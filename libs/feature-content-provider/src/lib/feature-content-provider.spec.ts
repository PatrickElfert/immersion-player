import { featureContentProvider } from './feature-content-provider';

describe('featureContentProvider', () => {
  it('should work', () => {
    expect(featureContentProvider()).toEqual('feature-content-provider');
  });
});
